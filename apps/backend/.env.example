# API Metro
METRO_API_BASE=https://api.metrolisboa.pt:8243/estadoServicoML/1.0.1/
METRO_API_KEY=
POLL_INTERVAL_MS=2000
DWELL_SECONDS=25

# Dev mock (optional): when set to 1, the ingestor will use a local JSON
# file instead of calling the Metro API.
# METRO_MOCK=1

# TLS (dev only): if your environment lacks the Metro API CA chain and you
# see UNABLE_TO_VERIFY_LEAF_SIGNATURE, you can temporarily disable TLS
# verification to confirm connectivity. Do NOT use in production.
# METRO_TLS_INSECURE=1

# Redis (Upstash)
REDIS_URL=
REDIS_TOKEN=
REDIS_TTL_SECONDS=15
REDIS_CHANNEL=metro:events
REDIS_SNAPSHOT_KEY=metro:snapshot

# CORS/Security
CORS_ORIGIN=http://localhost:5173
PORT=8080
